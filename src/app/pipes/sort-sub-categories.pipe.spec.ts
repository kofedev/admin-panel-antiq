import { SortSubCategoriesPipe } from './sort-sub-categories.pipe';

describe('SortSubCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortSubCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
