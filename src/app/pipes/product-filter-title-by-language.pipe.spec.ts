import { ProductFilterTitleByLanguagePipe } from './product-filter-title-by-language.pipe';

describe('ProductFilterTitleByLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFilterTitleByLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
