import { SortProductsByTitlePipe } from './sort-products-by-title.pipe';

describe('SortProductsByTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SortProductsByTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
