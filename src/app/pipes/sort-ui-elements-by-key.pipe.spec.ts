import { SortUiElementsByKeyPipe } from './sort-ui-elements-by-key.pipe';

describe('SortUiElementsByKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new SortUiElementsByKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
