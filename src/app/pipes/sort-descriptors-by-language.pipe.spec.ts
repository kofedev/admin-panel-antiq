import { SortDescriptorsByLanguagePipe } from './sort-descriptors-by-language.pipe';

describe('SortDescriptorsByLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new SortDescriptorsByLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
